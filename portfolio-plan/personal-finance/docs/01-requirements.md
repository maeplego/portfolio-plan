# P14 要件定義書

| 項目 | 値 |
| --- | --- |
| プロジェクト | P14 personal-finance |
| 対象スライス | 受け入れはスライス 1。2 以降は計画であり本書類の合格条件に含めない |
| 最終更新 | 2026-08-19 |
| 矛盾時の正 | 自動テストと製品コード、次に `DESIGN.md` |

## 1. 背景と目的

レシート直後に素早く記録し、月の使いすぎを早く知りたい。通信が不安定でも入力を失いたくない、というのが製品全体の動機だが、**スライス 1 はオンライン入力と月次の見え方**に絞る。オフラインの正（IndexedDB）は次スライス。

学習用であり、本番家計アプリや銀行接続の置き換えではない。実家計・実カード番号は使わない。

## 2. スコープ

### 含む（スライス 1 で満たす）

- 取引の CRUD（日付、正の整数円、カテゴリ、メモ、支出/収入）
- 月次一覧、カテゴリ円グラフ、日次棒グラフ
- 月次の全体予算と進捗バー
- 架空 3 ヶ月シード（2026-06〜08）
- 他ユーザーの取引が API で取れないこと
- PWA としてインストールできること（manifest + Service Worker）
- 単体 Compose（Postgres + API + Web）

### 含まない（意図的。計画スライスでも「できた」扱いにしない）

| 項目 | 理由 |
| --- | --- |
| IndexedDB が正 / オフラインキュー | DESIGN 実装順 3 |
| `POST /sync`、LWW、tombstone | 実装順 4 |
| CSV、繰り返し取引、複数ウォレット | 実装順 5 / 推奨機能 |
| P01 OIDC、アカウント削除、ダークモード | 実装順 6 |
| 銀行 API、確定申告、世帯権限、E2E 暗号化 | 非目標 |
| P15 との統合、K8s | DESIGN |

## 3. アクター

| アクター | 定義 | 認証（現状） |
| --- | --- | --- |
| 個人ユーザー | 自分の家計だけを見る | `X-Dev-User-Sub` |
| デモユーザー `demo` | 架空シードが入る | 同上 |

## 4. 前提・制約

- ID は ULID。金額は整数円。DB 時刻は `timestamptz`（UTC）。取引日は `DATE`（暦日）
- 通貨は JPY 固定
- 一人 1 家計。共有世帯は発展
- K8s に載せない。Compose のみ

## 5. 機能要件

| ID | 要件 | なぜ |
| --- | --- | --- |
| FR-01 | 金額は正の整数円。浮動小数は 400 | 円の一貫性。浮動小数禁止 |
| FR-02 | 取引 CRUD は所有者だけ | 認可はサーバ |
| FR-03 | 月次レポートは収入・支出・差引・カテゴリ・日次を整数で返す | グラフの入力 |
| FR-04 | 月次予算（全体）と残り（予算 − 支出） | 使いすぎを早く知る |
| FR-05 | `demo` に架空 3 ヶ月が入る。実データではない | デモとプライバシー |
| FR-06 | Web はインストール可能な PWA | クライアント品質の見せ場の入口 |

## 6. 非機能要件

| ID | 要件 | なぜ |
| --- | --- | --- |
| NFR-01 | 純論理と HTTP テストは DB なしで緑 | CI が Docker 無しでも回帰する |
| NFR-02 | Compose は専用 Postgres | 単独デモ |
| NFR-03 | README に学習用・実家計なし・既知の制限を書く | 誤用と過大広告を防ぐ |
| NFR-04 | HTTPS 前提。localhost は PWA の例外 | 仕様 |

## 7. 受け入れ（スライス 1）

1. `demo` の 2026-06 支出が 97780 円、収入 280000 円、予算残り 102220 円
2. `amountYen: 12.5` の POST が 400
3. ユーザー A の取引を B が list / get / delete できない（404 または空）
4. Web で月を切り替え、取引追加、グラフ表示ができる
5. `manifest.webmanifest` と Service Worker があり、localhost でインストールできる
6. `docker compose up` で上記が動く
