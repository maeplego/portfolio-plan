# Agent bootstrap

Do not preload everything. Read the map, then open only what the task needs.

1. This file (index)
2. `DESIGN.md` — relevant sections
3. `chat-context/CURRENT.md` if present — never all archive logs
4. Long standards docs — only the section you need

Skills (from agent-harness): `start-project`, `verify-before-commit`, `write-docs`, `record-chat-context`.

## Product / packages

| Path | Tests |
| --- | --- |
| `.` | _(fill in)_ |

## Notes

- `DESIGN.md` and `docs/` are git-managed; `chat-context/` and `.env` are not
