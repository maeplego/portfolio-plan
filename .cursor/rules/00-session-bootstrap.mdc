---
description: セッション開始時は地図だけ読む。全履歴の先読み禁止
alwaysApply: true
---

# セッション開始

1. Pxx を特定 → その `AGENTS.md`（索引）
2. `DESIGN.md` の該当節 + 対象 `pf-*` コード
3. `chat-context/CURRENT.md` があれば読む。過去ログは全読しない

詳細手順は skill `start-pxx`。正本の長い規約は `portfolio-plan/01-instructions.md`（必要な節だけ）。
