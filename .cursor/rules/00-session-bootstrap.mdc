---
description: 会話開始時に読むファイルとプロジェクト特定の手順
alwaysApply: true
---

# セッション開始

実装・調査の前に `AGENTS.md` の順で読む。チャット履歴だけに頼らない。

「P01 を実装して」の読み込み対象は `identity-platform/AGENTS.md`。

`DESIGN.md` は git 管理する。`chat-context/` は管理しない。製品コードは兄弟の製品リポジトリに置く。
