---
description: 秘密管理・テスト後コミット・危険作業の禁止
alwaysApply: true
---

# 品質（不変）

- 秘密は環境変数のみ。`.env` / 鍵 / `chat-context/` をコミットしない
- テストを書き、通ってから git に残す。`--no-verify` 禁止
- コミットは意図の単位。メッセージは「なぜ」
- 脆弱性の exploit / PoC は書かない
- 正式書類は `portfolio-plan/02-documentation.md`（skill `write-docs`）
